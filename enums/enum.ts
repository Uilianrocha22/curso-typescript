enum Planets {
  MERCURIO = "mercurio",
  VENUS = "venus",
  TERRA = "terra",
  MARTE = "marte",
}

enum Roles {
  ADMIN = 0,
  USER = "uilian",
}

console.log(Planets.TERRA);
console.log(Roles.USER);
